*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/build/
/.kotlin/
/dist/*.idsig

# Signing keys: the release key is this app's identity and the repo is public.
/keystore/
