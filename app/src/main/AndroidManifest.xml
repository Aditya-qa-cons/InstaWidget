<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      No permissions are declared here on purpose.

      * INTERNET is not needed: the app never makes a network call.
      * BIND_NOTIFICATION_LISTENER_SERVICE is a signature permission held by the
        system; it is enforced *on* our service via android:permission below,
        it is not something the app requests.
      * The user grants notification access manually from
        Settings > Notification access (see SetupActivity).

      RECEIVE_BOOT_COMPLETED is the one permission the app does ask for. It is
      not for doing work at boot: it only lets the app ask the system to
      rebind the notification listener, which several OEM builds otherwise
      leave dead after a restart, freezing the widget until the app is opened
      by hand. It grants no access to data and still makes no network calls.
    -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
      Instagram has to be declared visible. From targetSdk 30 the platform
      hides other packages by default, which makes resolveActivity() on
      instagram://direct_inbox return null even when Instagram is installed.
      The widget would then quietly fall back to the web inbox in a browser
      instead of opening the app.
    -->
    <queries>
        <package android:name="com.instagram.android" />
        <package android:name="com.instagram.lite" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="instagram" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".SetupActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Invisible: opens Instagram and finishes. NoDisplay draws nothing,
          noHistory and excludeFromRecents keep it out of the task switcher.
        -->
        <activity
            android:name=".OpenInboxActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.NoDisplay" />

        <!--
          Reads Instagram's own DM notifications. The system binds this, which is
          why it is exported and guarded by the signature-level bind permission.
        -->
        <service
            android:name=".DmNotificationListener"
            android:exported="true"
            android:label="@string/listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Backs the scrollable list inside the widget. -->
        <service
            android:name=".DmWidgetService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <!--
          Exported on purpose. The platform delivers APPWIDGET_UPDATE to this
          component either way, but some OEM launchers build their own widget
          picker by calling PackageManager.queryBroadcastReceivers from the
          launcher process, which filters out non-exported receivers and makes
          the widget invisible in the picker. Nothing sensitive is reachable
          this way: the receiver only redraws the widget from the local cache.
        -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".DmWidgetProvider"
            android:exported="true"
            android:label="@string/widget_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/dm_widget_info" />
        </receiver>

    </application>

</manifest>
